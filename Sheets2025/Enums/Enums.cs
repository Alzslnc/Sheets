namespace Sheets
{
    public enum ExtentsCreateType
    { 
        area,
        curve
    }
    public enum Action
    { 
        none,
        Ok, 
        Cancel,   
        NLC,
        Delete,
    }

    public enum PrefixType
    { 
        none,
        Layer,
        List,
        Attribute,
        Manual,
    }
}
