namespace Sheets
{
    public enum ExtentsCreateType
    { 
        area,
        curve
    }
    public enum NumType
    {
        none,
        order,
        byList,
        byReferenceAtteibute,
    }
    public enum PositionType
    {        
        point,
        viewport
    }
}
