namespace Sheets
{
    public enum Action
    { 
        none,
        Ok, 
        Cancel,   
    }

    public enum PrefixType
    { 
        none,
        Layer,
        List,
        Attribute,
        Manual,
    }
}
