namespace Sheets
{
    public enum Action
    { 
        none,
        Ok, 
        Cancel,   
        NLC,
        Delete,
    }

    public enum PrefixType
    { 
        none,
        Layer,
        List,
        Attribute,
        Manual,
    }
}
